 @model IEnumerable<AddressModel>

 <h1>Addresses</h1>
 <ul> 
     @foreach (var address in Model) 
     {  
         <li>@address.StreetAddress, @address.City, @address.State</li> 
     } 
 </ul> 

 @if (Model.Count() == 0) 
 { 
     <p>No addresses found.</p> 
 }
